# 白盒测试用例设计表

测试对象：personal_assistant.py 中的 PersonalAssistant 类
测试工具：pytest 8.4.2 + pytest-cov 7.1.0（分支覆盖模式 --cov-branch）
测试结果：6 / 6 PASSED，语句覆盖 100%，分支覆盖 100%（65 语句 / 34 分支）

---

## 一、测试目标与覆盖准则

依据《软件质量保证与测试》白盒测试要求，本次实验对 6 个被测方法采用以下覆盖准则组合，强度由低到高逐级叠加：

| 准则 | 含义 | 本实验应用 |
|---|---|---|
| 语句覆盖 SC | 每条可执行语句至少执行一次 | 所有方法体内语句全部命中 |
| 判定覆盖 DC | 每个判定的真/假分支都至少执行一次 | 每个 if 的 True/False 路径均被覆盖 |
| 条件覆盖 CC | 每个判定中每个原子条件取真/假各一次 | 针对 or / 闭区间等复合判定专门设计用例 |
| 判定-条件覆盖 DCC | 同时满足 DC 与 CC | 见 BMI / Sleep / Budget 用例 |
| 边界值 BVA | 每个数值阈值上取「正好等于、阈值±最小增量」 | 所有数值阈值均设计了 3 点用例 |

---

## 二、被测方法控制流分析

| 方法 | 判定数 | 原子条件数 | 独立路径数 | 数值阈值 |
|---|---|---|---|---|
| check_transaction | 2 | 2 | 3 | -5000, +10000 |
| validate_diary | 1（复合） | 2 | 2 | 10, 200 |
| evaluate_water_intake | 3 | 3 | 4 | 0, 1000, 2000 |
| evaluate_bmi | 4（1 复合 + 3 单一） | 5 | 5 | 0(h/w), 18.5, 24, 28 |
| check_monthly_budget | 3 | 3 | 4 | 0(budget), 0(spent), 0.8, 1.0 |
| evaluate_sleep | 3（1 复合 + 2 单一） | 4 | 4 | 0, 6, 9, 24 |
| 合计 | 16 | 19 | 22 | — |

---

## 三、白盒测试用例设计表

### 表 1：check_transaction（收支限额检查）

| 编号 | 输入 amount | 期望返回 | 覆盖目标 | 命中分支 |
|---|---|---|---|---|
| W1-1 | -100 | True | 正常支出，覆盖最后 return | 两个 if 均 False |
| W1-2 | 500 | True | 正常收入 | 两个 if 均 False |
| W1-3 | -5000 | True | 支出边界（=阈值，未越界） | if1 False |
| W1-4 | -5000.1 | False | 支出超额（阈值-ε） | if1 True |
| W1-5 | 10000 | True | 收入边界（=阈值，未越界） | if2 False |
| W1-6 | 10000.1 | False | 收入超额（阈值+ε） | if2 True |

覆盖说明：6 个用例满足语句覆盖（3 条 return 均执行）+ 判定覆盖（2 个 if 真假各 1）+ 边界值（-5000、10000 两侧各 1）。

---

### 表 2：validate_diary（日记字数规范）

判定为复合条件 `MIN <= len <= MAX`，等价于 `(len >= 10) AND (len <= 200)`，需做条件覆盖。

| 编号 | 输入 content（strip 后长度） | 期望返回 | 覆盖目标 |
|---|---|---|---|
| W2-1 | "今天天气真好，去公园散步了。"（14） | True | 区间内常规值 |
| W2-2 | "   这是一段带有首尾空格的日记内容   "（15） | True | strip 路径（空格干扰） |
| W2-3 | "一二三四五六七八九十"（10） | True | 下边界 =MIN |
| W2-4 | "小于十字"（4） | False | 下边界-，A False / B True |
| W2-5 | "          "（0） | False | strip 后为空 |
| W2-6 | "A" * 200（200） | True | 上边界 =MAX |
| W2-7 | "A" * 201（201） | False | 上边界+，A True / B False |

覆盖说明：W2-3 / W2-6 覆盖闭区间边界；W2-4 与 W2-7 让复合判定中两个原子条件各取 False，实现条件覆盖 CC。

---

### 表 3：evaluate_water_intake（饮水量评级）

控制流：if<0 → if>=2000 → if>=1000 → 默认 Danger，共 4 条独立路径。

| 编号 | 输入 water_ml | 期望返回 | 覆盖路径 |
|---|---|---|---|
| W3-1 | -500 | "Invalid" | 路径①：if1 True |
| W3-2 | 2500 | "Healthy" | 路径②：if1F → if2T |
| W3-3 | 2000 | "Healthy" | 健康线边界（=2000） |
| W3-4 | 1500 | "Warning" | 路径③：if1F → if2F → if3T |
| W3-5 | 1000 | "Warning" | 预警线边界（=1000） |
| W3-6 | 500 | "Danger" | 路径④：三 if 均 False |
| W3-7 | 0 | "Danger" | 零边界（合法最小值） |

覆盖说明：7 个用例命中全部 4 条独立路径 + 3 个数值阈值边界。

---

### 表 4：evaluate_bmi（BMI 评级）

首个判定为复合 `height_m <= 0 or weight_kg <= 0`，需对 or 两个原子条件分别做 True。

| 编号 | 输入 (height_m, weight_kg) | 计算 BMI | 期望返回 | 覆盖目标 |
|---|---|---|---|---|
| W4-1 | (0, 60) | — | "Invalid" | or 左条件 True（h<=0） |
| W4-2 | (1.70, 0) | — | "Invalid" | or 右条件 True（w<=0） |
| W4-3 | (-1.70, 60) | — | "Invalid" | 负值非法 |
| W4-4 | (1.80, 55) | 16.98 | "Underweight" | 偏瘦区间 |
| W4-5 | (1.70, 60) | 20.76 | "Normal" | 正常区间内 |
| W4-6 | (2.0, 74) | 18.5 | "Normal" | 正常下边界（=18.5） |
| W4-7 | (1.70, 75) | 25.95 | "Overweight" | 超重区间内 |
| W4-8 | (2.0, 96) | 24.0 | "Overweight" | 超重下边界（=24.0） |
| W4-9 | (1.70, 90) | 31.14 | "Obese" | 肥胖区间内 |
| W4-10 | (2.0, 112) | 28.0 | "Obese" | 肥胖下边界（=28.0） |

覆盖说明：W4-1、W4-2 分别使复合条件两个原子各为真，达成条件覆盖；W4-6 / W4-8 / W4-10 覆盖三个分级边界；5 个 return 全部命中。

---

### 表 5：check_monthly_budget（月度预算预警）

| 编号 | (spent, budget) | ratio | 期望返回 | 覆盖目标 |
|---|---|---|---|---|
| W5-1 | (100, 0) | — | "Invalid" | budget=0 非法边界 |
| W5-2 | (100, -500) | — | "Invalid" | budget<0 |
| W5-3 | (-50, 1000) | — | "Safe" | spent<0 短路分支 |
| W5-4 | (500, 1000) | 0.5 | "Safe" | 安全区间常规值 |
| W5-5 | (799, 1000) | 0.799 | "Safe" | 预警下边界- (0.8-ε) |
| W5-6 | (800, 1000) | 0.8 | "Warning" | 预警下边界 (=0.8) |
| W5-7 | (1000, 1000) | 1.0 | "Warning" | 超支线边界 (=1.0) |
| W5-8 | (1001, 1000) | 1.001 | "Over" | 超支线+ (1.0+ε) |
| W5-9 | (3000, 1000) | 3.0 | "Over" | 严重超支 |

覆盖说明：4 条独立路径全部命中；2 个比值阈值（0.8、1.0）各设计「-ε / =阈值 / +ε」三点。

---

### 表 6：evaluate_sleep（睡眠时长评级）

首个判定为复合 `hours<0 or hours>24`。

| 编号 | 输入 hours | 期望返回 | 覆盖目标 |
|---|---|---|---|
| W6-1 | -1 | "Invalid" | or 左条件 True（h<0） |
| W6-2 | 24.1 | "Invalid" | or 右条件 True（h>24） |
| W6-3 | 0 | "Insufficient" | 合法下边界（=0） |
| W6-4 | 5.9 | "Insufficient" | 不足区间- |
| W6-5 | 6 | "Healthy" | 健康下边界（=6） |
| W6-6 | 7.5 | "Healthy" | 健康区间内 |
| W6-7 | 9 | "Healthy" | 健康上边界（=9） |
| W6-8 | 9.1 | "Excessive" | 过量区间 |
| W6-9 | 24 | "Excessive" | 合法上边界（=24） |

覆盖说明：W6-1 / W6-2 实现 or 复合判定的条件覆盖；W6-3 / W6-5 / W6-7 / W6-9 覆盖全部 4 个数值边界。

---

## 四、测试执行命令

基础执行：

    python -m pytest test_assistant.py -v

含分支覆盖率统计（白盒报告核心数据）：

    python -m pytest test_assistant.py --cov=personal_assistant --cov-branch --cov-report=term-missing

生成 HTML 报告（用于实验报告截图）：

    python -m pytest test_assistant.py --cov=personal_assistant --cov-branch --cov-report=html

HTML 报告路径：D:\software quality testing\htmlcov\index.html

---

## 五、覆盖率结果

| 指标 | 数据 |
|---|---|
| 语句总数 / 命中 | 65 / 65 |
| 分支总数 / 命中 | 34 / 34 |
| 语句覆盖率 | 100% |
| 分支覆盖率 | 100% |
| 用例数 | 6 个测试函数，共计 47 条断言 |
| 测试结果 | 6 passed in 0.11s |
| Missing 行 | 无 |

pytest-cov 终端原始输出：

    Name                    Stmts   Miss Branch BrPart  Cover   Missing
    -------------------------------------------------------------------
    personal_assistant.py      65      0     34      0   100%
    -------------------------------------------------------------------
    TOTAL                      65      0     34      0   100%

---

## 六、白盒测试结论

1. 6 个被测方法均通过语句覆盖、判定覆盖、条件覆盖三级准则，未发现逻辑缺陷。
2. 分支覆盖率达到 100%，pytest-cov 报告 Missing 行为空，证明测试用例对每个 if 真假分支均有命中。
3. 所有数值阈值均设计了「-ε / =阈值 / +ε」三点边界用例，可有效防御「< 误写为 <=」「>= 误写为 >」等典型缺陷。
4. 针对复合判定（or、闭区间），单独构造了使每个原子条件分别取真的用例，达成条件覆盖（CC），强度高于普通判定覆盖。
5. 47 条断言全部通过，被测代码具备良好的可测性与正确性，可作为后续黑盒测试的稳定基线。
